{
  "_readme": [
    "워크플로 준비상태 원장 — '자격/설정이 없어 job이 통째로 skip됐는데 run은 초록'인 경로의 SSOT.",
    "가드: tools/check-workflow-readiness.ts (정적 = ci.yaml gate · 런타임 = 각 워크플로의 accounting job).",
    "산문 SSOT: CONTRIBUTING.md '워크플로 준비상태 회계' 절 · docs/traps-detail.md 「GHA job-level skip은 run conclusion에 안 보인다」 항목.",
    "",
    "왜 원장인가: GHA job conclusion 어휘(success|failure|cancelled|skipped)에는 '자격이 없어 안 돌았다'가",
    "없다. 스텝-레벨로 게이트된 job은 스텝을 전부 skip해도 job이 **success**로 끝난다(2026-07-27 라이브",
    "실측: tf-reconcile drift-github/drift-tailscale). 그래서 '무엇이 반드시 돌아야 하는가'를 여기 선언하고,",
    "accounting job이 needs.*.result / needs.*.outputs.executed로 매 run 대조한다.",
    "",
    "state 어휘 — **선언되지 않은 준비상태 게이트는 정적 가드가 red로 막는다**(미선언 = 통과 불가):",
    "  required     … 반드시 실행돼야 한다. 미실행 = severity에 따라 error(exit 1) 또는 warning(telegram).",
    "  unconfigured … 자격 미등록이 **알려진 갭**이다. since·owner_action 필수. 경고만 내고 run은 초록.",
    "                 (선언은 면제가 아니라 **계상**이다 — 매 run 로그·job summary에 남고 원장이 이유를 진다.)",
    "  optional     … 조건부 skip이 **설계상 정당**하다(백스톱이 따로 있다). 회계 대상 아님.",
    "severity는 required 전용이다: error=보안/권위 경로(run을 빨갛게) · warning=가용성(telegram만).",
    "expect_executed는 required 항목 수와 **일치해야 한다**(정적 가드가 강제) — 원장에서 항목이 조용히",
    "사라지는 열거 붕괴를 막는 바닥값이다."
  ],
  "workflows": {
    "tf-reconcile.yaml": {
      "accounting_job": "accounting",
      "expect_executed": 3,
      "jobs": {
        "reconcile": {
          "state": "required",
          "severity": "error",
          "why": "cloudflare DNS/tunnel 수렴의 최후 보루 — push-apply(iac.yaml)가 취소·실패해도 30분마다 수렴시킨다. CF 토큰/R2 키가 회전 중이거나 회수되면 apps.json↔라이브 드리프트가 조용히 정지한다(teardown 후 DNS 자동 회수·create-app 공개가 모두 멈춘다)."
        },
        "drift-github": {
          "state": "required",
          "severity": "error",
          "why": "github 루트(branch protection contexts=[\"gate\"]·strict · bump-poll writer-only ruleset · CI Actions 시크릿 2종)의 plan-only 드리프트 감시. 이 넷이 **terraform이 소유한** 신뢰 앵커다 — ⚠️ owner 경계 자체(`vars.HOMELAB_OWNER`)와 `vars.HOMELAB_DOMAIN`은 repo **variable**이라 terraform 밖이고 이 감시 범위에 없다(선언된 갭). 그 둘은 `policy/gh-secret-classification.json`의 `vars` 배열이 tracked 원장으로 지고, 부재·불일치는 15사본 actor 가드의 fail-closed 거부 + `homelab doctor` gh-owner가 진다 — terraform 관리로 올리지 않는 이유는 값이 gitignored `terraform.tfvars`에 남아 기대값이 tracked로 오지 않고, 읽기 전용 CI 자격의 Variables 읽기 권한이 미검증이라 아래 `github_repository`와 같은 허위 드리프트를 다시 부를 수 있어서다. — contexts에서 `gate`가 빠지면 **CI 없이 모든 PR이 머지**되고, ruleset의 bypass actor가 바뀌면 낡은 auto-merge 인가를 단 bump-poll/* 브랜치에 아무나 push할 수 있다. tests/gates/test_branch_protection.bats는 `.tf`가 뭐라 적혀 있는지만 보는 **정적** 가드라 라이브 이탈은 못 잡는다 — 그 간극이 이 job의 존재 이유다. 2026-08-19 자격 등록 + `-target` 배선으로 갭이 닫혔다(그 전까지 TF_GITHUB_TOKEN 부재로 **한 번도 실행된 적이 없었고**, 스텝-레벨 게이트라 job은 매 30분 초록이었다). 🔴 **감시 범위에 선언된 구멍이 하나 있다: `github_repository` 2건(homelab·app_template)은 읽기 전용 자격으로 감시할 수 없다.** GitHub은 repo의 merge 설정(allow_squash_merge· allow_auto_merge·delete_branch_on_merge…)을 Administration:**write** 토큰에게만 응답에 싣고, provider가 부재를 `false`로 읽어 `false -> true` **영구 허위 드리프트**를 만든다(2026-08-19 실측: 같은 시점 owner R&W 토큰은 \"No changes\", 읽기 전용 PAT는 \"2 to change\"). 403이 아니라 **조용한 오답**이라 더 나쁘다. write를 주는 우회는 금지 — 이 job이 지키는 대상이 바로 그 앵커인데 그걸 다시 쓸 수 있는 자격을 CI에 놓는 것이 된다. ⇒ 읽히는 4건만 `-target`으로 감시하고, 그 목록이 리소스 집합과 어긋나면 infra/github/test_ci_targets.bats가 막는다(뮤테이션 4종 확인). ⚠️ 감시 대상이 `github_repository` **리소스**를 참조하면 `-target`이 의존성으로 그것을 끌어와 허위 드리프트가 돌아온다 — `var.repo_name`·`data.github_repository`를 쓴다(같은 bats가 잠근다)."
        },
        "drift-tailscale": {
          "state": "required",
          "severity": "error",
          "why": "tailscale 루트(ACL·auth-key·tailnet 전역 DNS nameserver)의 plan-only 드리프트 감시. 내부 인입 경계와 노드 접근 ACL, 그리고 전 tailnet 기기의 이름해석이 이 루트의 선언에 걸려 있는데 수렴은 owner 로컬 apply로만 일어난다 — CI 감시가 멈추면 드리프트가 다음 로컬 apply까지 무기한 보이지 않는다(그 사이 admin console에서 ACL이 바뀌어도 신호 0). 2026-08-19 갭이 닫혔다: 읽기 전용 OAuth client(policy_file:read·dns:read·oauth_keys:read) 등록 + provider 스코프 주입(TF_VAR_ts_oauth_scopes) + terraform 핀 정렬. ⚠️ 이 job이 다시 조용해지는 경로는 셋이다 — (a) TF_TAILSCALE_OAUTH_ID/R2_* 회수·회전, (b) 워크플로의 스코프 주입 소실 → provider가 write 집합을 요청해 403, (c) owner 로컬 apply가 state writer 버전을 올려 CI terraform 핀이 낡음. (b)(c)는 infra/tailscale/test_provider_scopes.bats가 정적으로 잠그고, (a)는 이 원장 항목이 잡는다."
        }
      }
    },
    "bump-poll.yaml": {
      "accounting_job": "accounting",
      "expect_executed": 2,
      "jobs": {
        "reconcile": {
          "state": "required",
          "severity": "error",
          "why": "낡은 auto-merge 인가 회수 — 워크플로 주석 자신이 '회수는 가용성이 아니라 **보안 속성**'이라고 못박은 대상이다(R-27/R-31). writer 자격이 비거나 회전 중이면 이 job이 통째로 skip되고, 그동안 bump-poll/* 브랜치의 무장된 PR이 낡은 머지 인가를 그대로 유지한다. **면제 불가** — tools/check-workflow-readiness.ts의 SECURITY_CRITICAL이 required+error를 정적으로 못박는다."
        },
        "poll": {
          "state": "required",
          "severity": "warning",
          "why": "GHCR 폴링 bump(가용성). reader/writer 자격 회전 구간에 이미지 bump가 정지하는데 CI는 초록이다. 백스톱 ImageDigestDrift는 (a) 새 빌드가 푸시된 앱에만 발화하고 (b) 원인을 'ArgoCD sync 지연/태그 write-back 고장'으로 오귀속한다 — 실제 원인(reader 자격 만료)에 도달하지 못한다. 배포를 막지는 않으므로 run은 초록으로 두고 telegram으로 알린다."
        }
      }
    },
    "renovate.yaml": {
      "accounting_job": "accounting",
      "expect_executed": 1,
      "jobs": {
        "renovate": {
          "state": "required",
          "severity": "error",
          "why": "주 1회 의존성·보안 갱신 PR의 유일한 생산자다. writer App 자격이 비면 갱신이 전면 정지하는데 신호가 0이고, **백스톱이 아예 없다**(bump-poll에는 ImageDigestDrift가 오귀속으로나마 있다). 주 1회 실행이라 red의 노이즈 비용도 낮다."
        }
      }
    },
    "iac.yaml": {
      "accounting_job": "accounting",
      "expect_executed": 1,
      "jobs": {
        "iac-plan": {
          "state": "required",
          "severity": "error",
          "why": "PR 머지 전 cloudflare plan 미리보기. 시크릿이 없으면 스텝이 전부 skip되고 job summary가 비는데, 리뷰어는 빈 summary를 '변경 없음'으로 오독한다(정보 부재가 정보로 읽힌다). required check는 아니지만 PR에 red로 보이는 것이 정확한 신호다. ⚠️ fork PR은 구조적으로 시크릿을 못 받으므로 accounting job 자신이 fork를 제외한다(원장 면제가 아니라 트리거 경계)."
        }
      }
    },
    "reusable-app-build.yaml": {
      "jobs": {
        "deploy-trigger": {
          "state": "optional",
          "why": "앱 레포가 HOMELAB_DISPATCH_APP 시크릿 쌍을 넘기지 않으면 즉시 디스패치를 skip하는데, 이건 **설계된 백스톱 경로**다 — homelab bump-poll 크론(10분)이 같은 일을 하므로 skip해도 배포가 멈추지 않는다(쌍 중 하나만 오는 부분 설정은 그 자리에서 exit 1로 이미 fail-closed). 게다가 workflow_call이라 accounting job을 두면 caller(앱 레포) 컨텍스트에서 도는데, homelab 원장이 외부 레포의 시크릿 등록 여부를 판정하는 것은 경계 위반이다."
        }
      }
    }
  }
}
