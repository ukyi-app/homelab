# apps/api/deploy/prod/values.yaml에서 pnpm gen:env로 생성됨 — 직접 수정 금지
# 로컬 이너루프 env. 실제 값은 클러스터의 SOPS secret에서 온다.

LOG_LEVEL=info
DATABASE_URL=postgres://dev:dev@localhost:5432/app_dev?sslmode=disable
# secret 출처: api-secrets  (로컬에서만 채우고 절대 커밋 금지)
