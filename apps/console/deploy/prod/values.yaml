image: { repo: ghcr.io/owner/console, tag: sha-0000000 }
kind: spa
replicas: 1
spa: { server: sws }
resources:
  requests: { cpu: 10m, memory: 16Mi }
  limits:   { cpu: 100m, memory: 32Mi }
route:
  host: console.home.ukyi.app
  paths: ["/"]
  public: false
