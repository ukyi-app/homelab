# --- Secrets & keys — NEVER commit (only SOPS-encrypted *.enc.yaml belong in git) ---
*.agekey
age.key
*.age
key.txt
keys.txt
.env
.env.*
!.env.example
!.env.secrets.example
*.decrypted.yaml
*.dec.yaml

# --- Terraform ---
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
backend.hcl
crash.log
override.tf
override.tf.json

# --- SOPS plaintext scratch ---
*.tmp.yaml

# --- Kubeconfig / cluster creds ---
kubeconfig
*.kubeconfig
.kube/

# --- Node / pnpm ---
node_modules/
dist/
build/
.turbo/
*.tsbuildinfo
.pnpm-store/

# --- Local Claude tooling / runtime state ---
.claude/

# --- OS / editor ---
.DS_Store
*.swp
.idea/
.vscode/*
!.vscode/extensions.json

# --- kustomize `--enable-helm` chart-pull cache (build artifact, never commit) ---
platform/*/prod/charts/
# Terraform provider lock files: regenerate per-platform; commit multi-platform locks deliberately
.terraform.lock.hcl
# 운영 런북은 로컬 전용 — public 레포에 올리지 않는다(사용자 결정 2026-06-11)
docs/runbooks/
