{
  "kind": "bugfix-verify-red",
  "bug": "bump-poll-duplicate-pr",
  "sha": "41c7bdc3704dd537e98bba8661a412ab99b1a364",
  "treeSha": "9d0d34fbf484bb0b6790724931d37ea091c308c4",
  "regression": {
    "exit": 1,
    "failed": true,
    "symptomTokenPresent": false,
    "outputTail": "…[10897 chars elided; window anchored on symptomToken]…\nlane does not disarm on the REBUILD path either (a DIRTY armed autoDeploy PR keeps its arming)\nnot ok 101 bump-poll never calls gh pr create directly (PR creation goes through ensure-bump-pr)\n# tags: regression\n# (in test file tests/gates/test_bump-poll-callsite.bats, line 494)\n#   `false' failed\n# duplicate bump PR: bump-poll.yaml still calls 'gh pr create' directly — PR 생성은 tools/ensure-bump-pr.ts(조회→결정→변이)를 통해서만\n# 117:              gh pr create --base main --head \"$branch\" \\\n# 122:              gh pr create --base main --head \"$branch\" \\\nnot ok 102 bump-poll never pushes the bump branch directly (a skip decision must mutate nothing)\n# tags: regression\n# (in test file tests/gates/test_bump-poll-callsite.bats, line 504)\n#   `false' failed\n# orphan bump branch: bump-poll.yaml still runs 'git push' itself — push는 ensure-bump-pr가 관측 뒤에 (lease와 함께) 해야 한다\n# 115:            git push -u origin \"$branch\"\nnot ok 103 bump-poll drives the PR step through tools/ensure-bump-pr.ts\n# tags: regression\n# (in test file tests/gates/test_bump-poll-callsite.bats, line 513)\n#   `false' failed\n# duplicate bump PR: bump-poll.yaml이 tools/ensure-bump-pr.ts를 호출하지 않는다 — 멱등 실행기가 배선되지 않으면 도구 GREEN이 프로덕션을 고치지 못한다\nnot ok 104 the bump branch name carries no RUN_ID (same bump converges to one branch)\n# tags: regression\n# (in test file tests/gates/test_bump-poll-callsite.bats, line 523)\n#   `false' failed\n# duplicate bump PR: bump-poll.yaml still derives the branch from RUN_ID — 같은 bump가 run마다 다른 브랜치를 만들어 조회 대상이 사라진다\n# 80:          RUN_ID: ${{ github.run_id }}\n# 97:            branch=\"bump-poll/${app}-${RUN_ID}\"\nnot ok 105 the bump step creates the branch, bumps the tag, commits, and only then calls ensure-bump-pr (in that order)\n# tags: regression\n# (in test file tests/gates/test_bump-poll-callsite.bats, line 547)\n#   `false' failed\n# 호출부 순서 계약: bun tools/ensure-bump-pr.ts 호출이 없다 — 실행기가 배선돼야 순서를 증명할 수 있다\n#   기대 순서: git checkout -b → bun tools/bump-tag.ts → git commit → bun tools/ensure-bump-pr.ts\nn"
  },
  "characterization": {
    "exit": 0,
    "green": true,
    "outputTail": "7 bespoke descriptor without autoDeploy is fail-closed (propose-pr, never auto bump)\nok 38 bump rewrites only image.tag in the app's values.yaml\nok 39 bump is idempotent (second run is a no-op)\nok 40 bump --digest records image.digest alongside the tag\nok 41 bump rejects a malformed digest with exit 2\nok 42 bump with same tag and digest is a no-op\nok 43 tag-only bump removes a stale digest (image must follow the new tag)\nok 44 bump refuses path traversal outside apps/\nok 45 bump workflow joins the global homelab-mutation queue (no pending loss)\nok 46 bump rejects an unknown flag with exit 2 (typo'd --digest must not silently drop the pin)\nok 47 bump --expect-current aborts when current tag differs (races-4 TOCTOU)\nok 48 bump --expect-current proceeds when current tag matches\nok 49 bump rejects a value-flag with no value (arity, F2 digest-pin downgrade class)\nok 50 bump rejects a value-flag whose value is another --flag (arity)\nok 51 bump: v1 repository_dispatch path fully removed (writeback-dispatch gone)\nok 52 bump --pin edits the inline repo:tag@digest scalar in a bespoke deployment.yaml\nok 53 bump --pin without --digest is refused (bespoke pins are always digest-pinned)\nok 54 bump --pin --expect-current aborts on a tag mismatch (TOCTOU, exit 3)\nok 55 bump --pin is idempotent (same tag+digest is a no-op)\nok 56 bump --pin refuses a descriptor outside platform/ (path traversal guard)\nok 57 bump also refreshes the digest-exporter APPS tag for an app already listed\nok 58 bump leaves the digest-exporter byte-identical for an app not listed in APPS\nok 59 bump-poll passes --expect-current to bump-tag after checkout main (TOCTOU guard)\nok 60 bump-poll still checks out main fresh before each branch (snapshot reset)\nok 61 expect-current is sourced from the planner snapshot, not re-read from live values.yaml (F2)\nok 62 bump-poll branches on the bespoke pin descriptor and passes --pin to bump-tag\nok 63 bump-poll git-adds the planner writePath (unifies apps and bespoke lanes)\n"
  },
  "repro": null,
  "note": "written by bugfix-status.mjs --verify-red (script-owned re-execution)"
}
