
### release r1
R-48 accept — Single flip violated: 자동 형제 close 제거(disarm만 유지). 형제 close는 F-2로 분리.

### release r2
R-49 accept — Release proof still pins the R-48-broken implementation: 락을 최종 HEAD(red 5de984e / green 1f0a536)로 재핀, baseline을 현재 118-테스트 트리로 재구성, flip 수동 증명(verification.md §0). WAIVED by user: 머신-소유 verify-record 잔여 — 생성기(bugfix-status.mjs)가 세션 중 스킬 교체로 환경에서 사라져 재현 불가.
