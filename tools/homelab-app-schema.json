{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "homelab-app-schema",
  "title": ".homelab.yaml — 외부 앱 레포의 자기선언 (onboard가 values.yaml로 변환)",
  "type": "object",
  "additionalProperties": false,
  "required": ["kind", "resources"],
  "properties": {
    "kind": { "enum": ["service", "worker", "static"] },
    "resources": {
      "type": "object",
      "additionalProperties": false,
      "required": ["requests", "limits"],
      "properties": {
        "requests": { "$ref": "#/definitions/rr" },
        "limits": { "$ref": "#/definitions/rr" }
      }
    },
    "route": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "public": { "type": "boolean", "default": false },
        "host": { "type": "string", "pattern": "^[a-z0-9.-]+$" },
        "paths": { "type": "array", "items": { "type": "string", "pattern": "^/" }, "minItems": 1 }
      }
    },
    "replicas": { "type": "integer", "minimum": 1, "maximum": 3 },
    "db": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "enabled": { "type": "boolean" },
        "migrateCmd": { "type": "array", "items": { "type": "string" }, "minItems": 1 }
      }
    },
    "env": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["name", "value"],
        "properties": {
          "name": { "type": "string", "pattern": "^[A-Z][A-Z0-9_]*$" },
          "value": { "type": "string" }
        }
      }
    },
    "allowPlaintext": {
      "type": "array",
      "items": { "type": "string", "pattern": "^[A-Z][A-Z0-9_]*$" }
    },
    "secrets": {
      "type": "array",
      "items": { "type": "string", "pattern": "^[a-z][a-z0-9-]*$" }
    },
    "probes": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "liveness": { "$ref": "#/definitions/probe" },
        "readiness": { "$ref": "#/definitions/probe" }
      }
    },
    "static": {
      "type": "object",
      "additionalProperties": false,
      "properties": { "server": { "enum": ["sws", "caddy"] } }
    },
    "deploy": {
      "type": "object",
      "additionalProperties": false,
      "properties": { "autoDeploy": { "type": "boolean", "default": true } }
    }
  },
  "definitions": {
    "rr": {
      "type": "object",
      "additionalProperties": false,
      "required": ["cpu", "memory"],
      "properties": {
        "cpu": { "type": "string", "pattern": "^[0-9]+m?$" },
        "memory": { "type": "string", "pattern": "^[0-9]+(Mi|Gi)$" }
      }
    },
    "probe": {
      "type": "object",
      "additionalProperties": false,
      "required": ["path"],
      "properties": { "path": { "type": "string", "pattern": "^/" } }
    }
  }
}
