apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Namespace-level NetworkPolicies for the apps namespace (Pass-5 Open Item #3). Auto-discovered by
# the platform-components ApplicationSet (path platform/*/prod); it sets namespace: prod + CreateNamespace.
namespace: prod

resources:
  - networkpolicies.yaml
