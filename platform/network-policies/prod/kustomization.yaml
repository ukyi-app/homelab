apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# 앱 namespace용 namespace 수준 NetworkPolicy. platform-components
# ApplicationSet(path platform/*/prod)이 자동 발견; namespace: prod + CreateNamespace 설정.
namespace: prod

resources:
  - networkpolicies.yaml
