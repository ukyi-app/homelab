cloudnative-pg:
  replicaCount: 1
  monitoring:
    podMonitorEnabled: false # VM operator 없음; annotation 기반으로 scrape (M5)
  resources:
    requests:
      cpu: 50m
      memory: 100Mi
    limits:
      cpu: 200m
      memory: 200Mi
  config:
    data:
      # barman-cloud plugin(CNPG-I) sidecar는 여기가 아니라 Cluster별로 활성화한다
      INHERITED_ANNOTATIONS: "prometheus.io/*"
