apiVersion: apps/v1
kind: Deployment
metadata: { name: adguard, namespace: edge }
spec:
  replicas: 1
  strategy: { type: Recreate } # RWO PVC, 단일 노드
  selector: { matchLabels: { app: adguard } }
  template:
    metadata: { labels: { app: adguard } }
    spec:
      automountServiceAccountToken: false # SA 토큰 불요(k8s API 미사용) — 토큰 탈취 표면 제거(보안 감사 SA-2)
      securityContext:
        # ⚠️ **hostPath 백엔드 PV에는 fsGroup이 적용되지 않는다**(kubelet의 소유권 관리 대상이 아니다 —
        #    2026-08-14 NUC 실측: PVC 디렉토리가 fsGroup 소유가 아니라 `root:root 0777`이었다).
        #    그래서 이 값에 기대지 말고, **PVC에 파일을 만드는 컨테이너가 65532로 돌아야 한다**(아래).
        #    디렉토리가 0777이라 65532도 생성은 되지만, root가 만든 **파일**은 0644 root 소유라
        #    뒤따르는 65532 컨테이너가 열지 못한다.
        fsGroup: 65532 # PVC(conf/work)를 65532 그룹 쓰기 가능으로 — non-root AdGuard의 설정 저장용
      initContainers:
        # ⚠️ **65532로 돌아야 한다.** root로 돌면 `cp`가 AdGuardHome.yaml을 `0644 root:root`로 만들고,
        #    다음 init인 inject-auth(65532)가 `permission denied`로 죽어 파드가 Init:CrashLoopBackOff에
        #    빠진다(2026-08-14 NUC 콜드스타트 실측 — 메인 컨테이너도 같은 이유로 설정을 못 쓴다).
        #    ⚠️ 라이브 Mac에서는 원리적으로 안 보인다: 파일이 이미 있어 `cp -n`이 건너뛰므로
        #       소유권이 문제될 일이 없다. **빈 PVC에서만 드러난다.**
        - name: seed-config
          image: busybox:1.38@sha256:dc2d74b28e4cf8984fa52af1f39bc7c3d9c73760b41a74d629f5d11b1ab28616
          command: ["sh", "-c", "cp -n /seed/AdGuardHome.yaml /opt/adguardhome/conf/AdGuardHome.yaml || true"]
          volumeMounts:
            - { name: seed, mountPath: /seed }
            - { name: data, mountPath: /opt/adguardhome/conf, subPath: conf }
          securityContext: # inject-auth와 동일 — 같은 uid가 만들고 같은 uid가 고쳐야 한다
            runAsNonRoot: true
            runAsUser: 65532
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true # cp는 마운트된 PVC에만 쓴다
            capabilities: { drop: ["ALL"] }
            seccompProfile: { type: RuntimeDefault }
        # GitOps 강제 UI 인증: AdGuard는 users:만 인증 수단(secret/env 네이티브 미지원)이라 PVC의
        # AdGuardHome.yaml .users를 SealedSecret의 bcrypt 해시로 매 시작 set한다(yq 멱등 — UI에서 바꿔도
        # 재시작 시 sealed 값으로 수렴, DR/재배포 일관). seed-config가 먼저 PVC를 채운 뒤 실행돼야 한다(순서 의존).
        # cp -n은 라이브의 기존 users:[]를 덮지 않으므로 이 단계가 라이브에도 인증을 적용하는 유일 경로다.
        - name: inject-auth
          image: mikefarah/yq:4.53.3@sha256:11a1f0b604b13dbbdc662260d8db6f644b22d8553122a25c1b5b2e8713ca6977
          command: ["yq"]
          args:
            - "-i"
            - '.users = [{"name": strenv(AGH_USER), "password": strenv(AGH_PW_HASH)}]'
            - /opt/adguardhome/conf/AdGuardHome.yaml
          env:
            - { name: AGH_USER, value: ukkiee } # username은 비밀이 아니다 — 평문 보관
            - name: AGH_PW_HASH
              valueFrom:
                secretKeyRef: { name: adguard-auth, key: PASSWORD_HASH }
          volumeMounts:
            - { name: data, mountPath: /opt/adguardhome/conf, subPath: conf }
            - { name: tmp, mountPath: /tmp } # yq -i는 /tmp에 임시파일을 쓴다 — readOnlyRootFilesystem과 양립시킴
          securityContext: # setcap 불필요 → 메인 컨테이너와 달리 restricted 완전 충족
            runAsNonRoot: true
            runAsUser: 65532
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
            seccompProfile: { type: RuntimeDefault }
      containers:
        - name: adguard
          image: adguard/adguardhome:v0.107.79@sha256:aba9e3bf0613be3ba3755e1fc311b126e2c24bec25e18b6483894a88283074f0
          ports:
            - { name: dns-udp, containerPort: 53, protocol: UDP }
            - { name: dns-tcp, containerPort: 53, protocol: TCP }
            - { name: http, containerPort: 3000 }
          volumeMounts:
            - { name: data, mountPath: /opt/adguardhome/conf, subPath: conf }
            - { name: data, mountPath: /opt/adguardhome/work, subPath: work }
          resources:
            requests: { cpu: 25m, memory: 48Mi }
            # right-size 2026-09-01(**해상도 정정 재측정**): A′ peak 124.4Mi(14일, 파드-세대 붕괴,
            #    30초 해상도) × 2.0x = 256Mi(2.06x).
            # ⚠️ 직전 값 160Mi의 근거였던 "A′ peak 79.0Mi"는 `[14d:5m]` 서브쿼리가 30초 스크레이프의
            #    90%를 버려서 나온 값이다 — 8/31 06:51Z에 한 시간짜리 124.4Mi 스파이크가 있었는데
            #    5분 격자에 한 점도 걸리지 않았다(5분 해상도로는 그 시간대가 78.6→73.5Mi로 보인다).
            #    ⭐ 128→192 선제 상향의 근거였던 "peak 123/128(96%)"은 working_set이라 #564로 무효화됐지만,
            #    30초로 다시 재니 A′ peak(124.4)가 그 값과 사실상 같다. 블록리스트 성장은 실재하는
            #    축이고 LAN 전체 DNS가 여기 걸려 있어 2.0x를 하한 그대로 쓴다.
            limits: { cpu: 200m, memory: 256Mi }
          # readinessProbe — 프로브가 없으면 kubelet은 프로세스가 뜨는 즉시 Ready로 두므로,
          # LanDnsPathDown(core.yaml)이 보는 `kube_deployment_status_replicas_ready`는 'DNS가 응답한다'가
          # 아니라 'adguardhome 프로세스가 살아 있다'를 잰다. AdGuard는 /metrics를 안 내 TargetDown도 없고
          # rewrite 리컨실러 하트비트는 HTTP API(:3000)만 증명한다 — 즉 "프로세스는 살아 있는데 DNS만
          # 멈춘" 클래스가 어느 알림에도 안 잡혔다. NotReady → replicas_ready 0 → 기존 LanDnsPathDown
          # (`for: 60s`)이 그대로 페이징하므로 룰 변경은 필요 없다.
          # ⚠️ 질의 도메인은 **rewrite로 로컬 응답되는 이름**이어야 한다(adguardhome.yaml의
          #    `*.home.ukyi.app` rewrite — 리컨실러가 관리하는 그 한 줄). 그래서 업스트림 장애와 무관하고,
          #    뒤집어 말하면 **업스트림 전건 실패(SERVFAIL)는 이 프로브로 안 잡힌다**. `adguard.home.ukyi.app`은
          #    이 컴포넌트 자신의 HTTPRoute 호스트라(httproute.yaml) 레포에 실재하는 이름이다.
          #    httpGet 대안은 없다 — `:3000/health-check`가 401(auth 강제)이다.
          # ⚠️ **livenessProbe는 의도적으로 두지 않는다.** replicas 1 + LoadBalancer(adguard-dns) 구조라
          #    kubelet이 재시작을 걸면 LAN 유일 리졸버가 그 동안 통째로 끊긴다. 특히 cpu limit 200m 아래
          #    블록리스트 갱신 스파이크(위 주석의 124Mi 한 시간)와 겹치면 exec 연속 실패 → 재시작 →
          #    같은 필터 로드 반복의 자기유발 루프가 된다. 교착의 재기동은 사람이 판단한다.
          #    (readiness도 같은 대가를 작게 진다: NotReady = 엔드포인트 이탈. timeoutSeconds 3 ·
          #     failureThreshold 3으로 30초 유예를 줘 일시 지연이 단절로 증폭되지 않게 한다.)
          # 가드: platform/adguard/prod/test_adguard_auth.bats
          readinessProbe:
            exec: { command: [nslookup, -type=a, adguard.home.ukyi.app, 127.0.0.1] }
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          securityContext:
            runAsNonRoot: true
            runAsUser: 65532
            # AdGuard 공식 이미지는 바이너리에 file capability(setcap)가 있어 NoNewPrivs
            # (allowPrivilegeEscalation:false)와 양립 불가 — exec EPERM으로 CrashLoop(라이브 확인).
            # :53 바인딩용 NET_BIND_SERVICE만 허용하고 나머지는 전부 드랍 유지.
            allowPrivilegeEscalation: true
            capabilities: { drop: ["ALL"], add: ["NET_BIND_SERVICE"] }
      volumes:
        - name: seed
          configMap: { name: adguard-config }
        - name: data
          persistentVolumeClaim: { claimName: adguard-data }
        - name: tmp
          emptyDir: {} # inject-auth yq의 /tmp 임시파일 전용(readOnlyRootFilesystem 양립)
