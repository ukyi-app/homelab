apiVersion: v1
kind: PersistentVolumeClaim
metadata: { name: adguard-data, namespace: edge }
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: standard
  resources: { requests: { storage: 1Gi } }
