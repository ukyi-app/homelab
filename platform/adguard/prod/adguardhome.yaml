apiVersion: v1
kind: ConfigMap
metadata: { name: adguard-config, namespace: edge }
data:
  AdGuardHome.yaml: |
    http:
      address: 0.0.0.0:3000
    dns:
      bind_hosts: ["0.0.0.0"]
      port: 53
      upstream_dns:
        - https://dns.cloudflare.com/dns-query   # DoH 업스트림
        - https://dns.quad9.net/dns-query
      bootstrap_dns: ["1.1.1.1", "9.9.9.9"]
      upstream_mode: load_balance
    filtering:
      rewrites:
        # split-horizon: 모든 *.home 이름 → Traefik tailscale passthrough 디바이스(traefik-ts LB).
        # Traefik이 :443에서 와일드카드 cert로 TLS 종단하므로 valid HTTPS로 접근된다.
        - domain: "*.home.ukyi.app"
          answer: "100.77.132.120"
      protection_enabled: true
    filters:
      - enabled: true
        url: https://adguardteam.github.io/HostlistsRegistry/assets/filter_1.txt
        name: AdGuard DNS filter
        id: 1
    schema_version: 27
