apiVersion: v1
kind: ServiceAccount
metadata: { name: vector }
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata: { name: vector }
rules:
  - apiGroups: [""]
    resources: ["pods", "namespaces", "nodes"]
    verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata: { name: vector }
roleRef: { apiGroup: rbac.authorization.k8s.io, kind: ClusterRole, name: vector }
subjects: [{ kind: ServiceAccount, name: vector, namespace: observability }]
---
apiVersion: v1
kind: ConfigMap
metadata: { name: vector-config }
data:
  vector.yaml: |
    data_dir: /vector-data
    sources:
      k8s:
        type: kubernetes_logs
      internal:                       # vector 자기 메트릭(backpressure/드롭 노출)
        type: internal_metrics
    transforms:
      parse:
        type: remap
        inputs: [k8s]
        source: |
          .stream = .stream
          .namespace = .kubernetes.pod_namespace
          .pod = .kubernetes.pod_name
          .container = .kubernetes.container_name
    sinks:
      vlogs:
        type: elasticsearch
        inputs: [parse]
        endpoints: ["http://victorialogs:9428/insert/elasticsearch"]
        mode: bulk
        api_version: v8
        compression: gzip
        healthcheck: { enabled: false }
        query:
          _msg_field: message
          _time_field: timestamp
          _stream_fields: namespace,pod,container,stream
        request:
          headers:
            AccountID: "0"
            ProjectID: "0"
      prometheus:                     # vector 자기 메트릭 /metrics:9598 노출(vmagent pod-annotations scrape)
        type: prometheus_exporter
        inputs: [internal]
        address: "0.0.0.0:9598"
---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: vector
  labels: { app.kubernetes.io/name: vector }
spec:
  selector:
    matchLabels: { app.kubernetes.io/name: vector }
  template:
    metadata:
      labels: { app.kubernetes.io/name: vector }
      annotations:                    # POD 레벨(.spec.template.metadata) — vmagent가 pod annotation을 discovery
        prometheus.io/scrape: "true"
        prometheus.io/port: "9598"
    spec:
      # ⚠️ automountServiceAccountToken을 false로 바꾸지 마라 — kubernetes_logs 소스가 파드 메타데이터
      #    (네임스페이스·라벨·컨테이너)를 API에서 읽어 로그에 붙인다. 토큰을 떼면 수집이 죽는다.
      serviceAccountName: vector
      # k3s가 쓰는 /var/log/pods/**/*.log는 root:root 0640이라 비root는 읽지 못한다 —
      # nobody(65534)로는 로그 수집이 전혀 되지 않는다. 노드 로그 수집 DaemonSet의
      # 표준대로 root로 실행한다(observability ns는 PSA privileged). 파일은 readOnly 마운트.
      securityContext: { runAsUser: 0, runAsGroup: 0, seccompProfile: { type: RuntimeDefault } }
      containers:
        - name: vector
          image: timberio/vector:0.56.0-distroless-libc@sha256:58c37f908648388a8a771426451476d60ebbd393fe52cc08a94573bcbb1c629a
          # `--threads` 핀 — vector는 tokio 워커를 노드 코어 수만큼 띄운다(NUC 14코어에서 15개 확인).
          # 기준선(Mac 6코어)에 묶어 두는 **위생 조치**다. env(VECTOR_THREADS) 대신 CLI 플래그를 쓰는
          # 이유는 이름이 틀리면 즉시 기동 실패로 드러나기 때문이다(env 오타는 조용히 무시된다).
          # ⚠️ **이것은 OOM의 원인이 아니었다**(2026-08-14 반증). 핀 적용 후 워커는 15→8로 줄었지만
          #    커널 OOM 기록의 anon-rss는 325324 kB로 **핀 전(325080·325136·325160·325056)과 동일**했다.
          #    코어 수를 원인으로 지목했던 최초 진단은 틀렸다 — 아래 limit 주석이 실제 원인이다.
          args: ["--config", "/etc/vector/vector.yaml", "--threads", "6"]
          ports: [{ name: metrics, containerPort: 9598 }]   # vector 메트릭 노출
          env:
            - { name: VECTOR_SELF_NODE_NAME, valueFrom: { fieldRef: { fieldPath: spec.nodeName } } }
          resources:
            # OOM 근본원인(2026-06-23, PR#107 seccomp 롤아웃이 방아쇠): 파드 '재생성' 시 data_dir이 emptyDir라
            # vector 체크포인트(파일별 읽기 위치)가 유실 → kubernetes_logs가 read_from=beginning으로 /var/log 백로그를
            # 처음부터 콜드 재리드(라이브 실측 50.9Mi 단발 버스트) → reclaimable page-cache가 얹혀 cgroup max_usage가
            # 256Mi 천장을 터치 → 스타트업 창에서 단발 OOMKilled. 근본수정=data_dir hostPath 영속화(아래 volumes)로
            # 재생성 콜드 재리드 자체를 제거. limit/request는 라이브 실측 재산정 — steady working_set ~145Mi(10m max)/
            # 172Mi(1h)·스타트업 피크 197Mi 기준: request 160Mi(steady 위로 eviction 보호)·limit 320Mi(cgroup 피크
            # 256Mi 위 헤드룸 + hostPath 적용 첫 부팅의 마지막 콜드리드 흡수). (원장 행 동반: req 1120→1152, limit 2144→2208.)
            # ⚠️ 320Mi → 512Mi (2026-08-14 NUC 실측, 2시간에 22회 OOMKilled).
            #    ⚠️ **원인은 sink 백프레셔지 로그량이 아니다.** vlogs(elasticsearch sink)가 죽어 있으면
            #       `Endpoint is unhealthy` + 무한 재시도로 미전송 배치가 메모리에 쌓인다.
            #       vlogs가 OOM 루프를 돌면 vector도 따라 죽는 **결합 루프**다 — vlogs 쪽 limit(위 256Mi)이
            #       1차 처방이고, 이 값은 그 루프가 다시 생겨도 견디는 여유다.
            #    ⚠️ 노드 코어 수/스레드 수는 원인이 아니었다(위 args 주석의 반증 참조).
            # ✅ **재기준선 완료 (2026-08-15, 버스트 후 27시간 steady 실측)** — 회수하지 않는다.
            #    cgroup 실측: `memory.current` **71Mi** · `memory.peak` **418Mi** / limit 512Mi (82%).
            #    peak는 이 limit으로 **실제 백로그 버스트를 겪으며 살아남은 값**이다(2026-08-14).
            #    ⚠️ **버스트 크기는 장애 지속시간에 비례한다** — 418Mi는 17시간 장애가 만든 것이고
            #    더 긴 장애면 더 크다. 즉 peak 위 94Mi는 낭비가 아니라 **더 나쁜 장애에 대한 보험**이다.
            #    steady(71Mi)에 맞춰 깎으면 관측이 가장 필요한 순간에 파이프라인이 죽는다.
            #    ⚠️ 예산도 쪼들리지 않는다: 원장 limit 합 9020 / cap 10240 = **명목 잔여 1220Mi**.
            #    회수를 다시 검토할 조건: 잔여가 신규 온보딩을 막는 수준(≈수십 Mi)까지 떨어질 때.
            #    그때는 limit을 깎기 전에 **버스트 자체를 줄일 것** — sink `request.concurrency`를
            #    고정값으로 묶어 미전송 배치의 상한을 만드는 쪽이 먼저다(미검증 — 넣으면 반드시
            #    같은 지표를 다시 재라).
            requests: { cpu: 50m, memory: 160Mi }
            limits: { memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
          volumeMounts:
            - { name: config, mountPath: /etc/vector }
            - { name: data, mountPath: /vector-data }
            - { name: varlog, mountPath: /var/log, readOnly: true }
            - { name: varlibdockercontainers, mountPath: /var/lib/docker/containers, readOnly: true }
      volumes:
        - { name: config, configMap: { name: vector-config } }
        # data_dir(/vector-data) 체크포인트를 노드 디스크에 영속화 — 파드 재생성·재부팅에도 보존돼 kubernetes_logs
        # 콜드 재리드를 제거(근본수정). DirectoryOrCreate=첫 부팅 자동 생성(root:root 0755, vector runAsUser:0이라 쓰기 OK).
        # VM 재구축(DR) 시 /var/log와 함께 소실되나 그때만 1회 콜드리드(백로그도 함께 비어 소규모).
        - { name: data, hostPath: { path: /var/lib/vector, type: DirectoryOrCreate } }
        - { name: varlog, hostPath: { path: /var/log } }
        - { name: varlibdockercontainers, hostPath: { path: /var/lib/docker/containers } }
