apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: homepage  # homepage-prod Application의 권위 있는 대상 namespace
resources:
  - rbac.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - httproute.yaml
  # networkpolicy.yaml은 Task 10에서 추가
