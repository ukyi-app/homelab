title: "ukyi · home"
headerStyle: underlined
target: _blank
quicklaunch:
  searchDescriptions: true
  hideInternetSearch: true   # 내부 전용 대시보드 — 등록 서비스/북마크 안에서만 검색
background:
  image: /images/background.jpg   # homepage-assets ConfigMap → /app/public/images (Task 5)
  blur: sm
  brightness: 75
  opacity: 50
hideVersion: true
statusStyle: dot
useEqualHeights: true
layout:
  Infra:
    style: row
    columns: 3   # 6타일(메트릭 4카드+Glances 2)을 3x2로 — 카드 폭 확보로 잘림 방지
    icon: mdi-server-network
  Platform:
    style: row
    columns: 4
    icon: mdi-cog
  Apps:
    style: row
    columns: 4
    icon: mdi-apps
