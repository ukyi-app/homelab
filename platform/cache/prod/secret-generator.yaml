apiVersion: viaduct.ai/v1
kind: ksops
metadata:
  name: cache-secret-generator
  annotations:
    config.kubernetes.io/function: |
      exec:
        path: ksops
files:
  - cache-r2-creds.enc.yaml # -> Secret cache-r2-creds (cache-backup CronJob의 rclone R2 업로드)
